Sometimes, we have many components in the app tree. Therefore, we have to pass props to every child component until it reaches the destinated component. This is messy since some components may not need the data, they just carry it. This where Context API comes in.

Context API is suitable for global state

video 2