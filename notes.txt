Let's say we want to share bgColor and textColor from Script component to Navbar and BookList components. In the past, we had to pass bgColor and textColor as props to Navbar and BookList (Script.jsx). Then we receive them as props in Navbar and BookList. This method is called Prop Drilling.

video 2